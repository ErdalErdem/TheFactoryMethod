public interface ShapeFactory {
    Shape create();
    // ShapeFactory is a functional interface that represents a factory method for creating shapes.
    // It has a single method create() which returns a Shape. This interface serves as a blueprint for creating different shapes.
}
