public class Circle implements Shape {
    @Override
    public void draw() {
        System.out.println("Drawing a Circle");
        // Circle and Square are concrete implementations of the Shape interface. Each has its own implementation of the draw() method.
    }
}
